spring:
  application:
    name: 测试系统111
# 核心配置
zymail:
  # 唯一id
  app-id: test-01
  # 邮件配置
  mail:
    host: smtp.163.com
    username: huangzheyu2024@163.com
    password: ZZIGGUATBITQVDJC
  # 服务端配置
  server:
    # 接入地址（一定要加协议名）
    url: http://localhost:8883
  # 强制连接server（连接失败则项目无法启动，默认false）
  force: true
server:
  # 项目端口号
  port: 8084